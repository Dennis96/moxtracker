# STEP 3 — rifinitura visiva dopo prova reale

Questa iterazione non modifica il backend.

- Corretto il selettore CSS che riduceva per errore il grande logo `MOX` del hero: ora il kicker mantiene davvero le dimensioni previste.
- Hero più vicino al concept approvato: MOX molto grande, `ARENA ASSISTANT` subito sotto, mascotte più presente.
- Contenitore desktop reso quasi full-width anche con browser zoomato: niente max-width rigido a 1760 px.
- Meta Explorer non viene più stirato verticalmente quando ci sono poche partite: cresce in modo naturale con le righe reali.
- Dashboard leggermente riequilibrata e più densa sui monitor larghi.
- Nessuna modifica a `src/`, API, schema o Worker.
